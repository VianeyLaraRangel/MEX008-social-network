let perfil = {
    render: async () => {
        let view =
        `<h1>AQUI VAMOS A VER EL PERFIL DEL USUARIO</h1>`

        return view,
        console.log("perfil del usuario");
    },
    after_render: async () => {}
}